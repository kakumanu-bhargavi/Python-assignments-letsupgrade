// 2.program to convert minutes to seconds
function convertMinutestoSeconds(minutes)
{
    return Math.floor(minutes * 60);
}
var minutesToSeonds = convertMinutestoSeconds(2);
console.log("result " +minutesToSeonds, "seconds")